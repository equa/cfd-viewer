# Everything, for development and for the resting Trame front end.
#
#   pip install -r requirements.txt
#
# The three.js service needs only requirements-core.txt -- no trame at all. The
# cfd-viz container image installs that file, which is why this one is a thin
# wrapper: one place per dependency, so the two cannot drift.
-r requirements-core.txt

# The RESTING Trame front end (main.py --trame) only. Nothing in server/ or
# web/ imports trame; drop these if that app is ever removed.
trame>=3.13
trame-vtk>=2.11
trame-vuetify>=3.2
trame-components>=2.5

# Only needed for the browser suites (tests/check_client.py, browser_check.py).
# Pillow is used there to count pixels in a screenshot.
# playwright>=1.40
# pillow>=10.0

# The browser client is built with node, not pip:
#   cd web && npm install && npm run build
